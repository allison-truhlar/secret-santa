import { Router } from "express";

const router = Router();

// POST a new group with members
router.post("/create", createGroup);

// READ a group and all members

// UPDATE a group

// DELETE a group or a single member
